

module.exports = function(){
  
  
  
  //handle dropping files from outside of the application to our applications
  window.ondragover = function(e) { e.preventDefault(); return false;  };
  window.ondrop = function(e) { e.preventDefault(); return false; };

  $(window.document).on('drop', '.folderView, #currentDirectory', function(e){
      e = e.originalEvent;
      e.preventDefault();
      console.log($(this).attr('data-path'));

      for (var i = 0; i < e.dataTransfer.files.length; i++){
        console.log(e.dataTransfer.files[i]);
        
        fs.renameSync(e.dataTransfer.files[i].path, path.join(start, e.dataTransfer.files[i].name));
        }

      return false;
    });
};